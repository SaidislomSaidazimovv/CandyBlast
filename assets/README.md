# Native artwork source

`logo.png` is the single Candy Blast source used to generate Android and iOS launcher icons and splash screens. The generated platform images are committed under `android/` and `ios/` so ordinary builds do not need an image-generation dependency.
